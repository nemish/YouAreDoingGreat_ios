name: Tests

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  test:
    name: Run Tests
    runs-on: macos-15  # macOS Sequoia with Xcode 16+

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Select Xcode version
        run: sudo xcode-select -s /Applications/Xcode_16.1.app

      - name: Show Xcode version
        run: xcodebuild -version

      - name: Install iOS 18.1 Simulator
        run: |
          sudo xcversion simulators --install='iOS 18.1' || xcodebuild -downloadPlatform iOS || echo "iOS 18.1 runtime installation skipped"

      - name: Build and test
        run: |
          # Use any iOS Simulator - let Xcode pick one
          xcodebuild test \
            -project YouAreDoingGreat.xcodeproj \
            -scheme YouAreDoingGreat \
            -destination 'platform=iOS Simulator,name=Any iOS Simulator Device' \
            -enableCodeCoverage YES \
            -resultBundlePath TestResults.xcresult

      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: TestResults.xcresult

      - name: Upload coverage report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: TestResults.xcresult
